<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yunxin.dao.RoomDAO">
<select id="querybyId" parameterType="Long" resultType="Long">
    SELECT user_id FROM room_user WHERE room_id=#{roomId}
</select>
</mapper>